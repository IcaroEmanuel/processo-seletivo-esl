<div class="row justify-content-between p-3 mb-3 bg-dark text-white fs-5" style="--bs-bg-opacity: .9;">
  <p class="col-6 d-flex text-center">Sistema de reservas de salas</p>

  <% if user_signed_in? %> 
    <div class="dropdown col-4 d-flex justify-content-end">
      <button class="btn btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
        Olá, <%= current_user.name %>
      </button>
      <ul class="dropdown-menu">
        <li><a class="dropdown-item" <%= link_to  'Sair', logout_path, method: :delete %></a></li>
      </ul>
    </div>
  <% end %>
</div>

 